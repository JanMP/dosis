
{ Meteor } = require  "meteor/meteor"
{ assert } = require  "meteor/practicalmeteor:chai"

describe "settings", ->
  it "does something that should be tested", ->
    testString = "blah"
    assert.equal testString, "blah"
